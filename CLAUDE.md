# ValveVision-PiArm

แขนกล 6 แกนบน Raspberry Pi 4 ใช้กล้องตรวจจับวาล์วเติมลมยาง แล้วยื่นไปแตะ
เป็น senior project มีวันสอบ — อยู่ระหว่าง rebuild ครั้งใหญ่ (ส.ค. 2026)

**อ่านก่อนเริ่มงาน:**
[CONTEXT.md](./CONTEXT.md) คำศัพท์กลาง → [DESIGN.md](./DESIGN.md) การตัดสินใจและเหตุผล → [PLAN.md](./PLAN.md) งานที่ต้องทำเรียงลำดับ พร้อม design ของแต่ละส่วน

งานที่ต้องใช้มือเจ้าของโปรเจ็ค (วัด จัดวาง label) แยกไว้ที่ [TODO.md](./TODO.md)

**ทำงานตาม `PLAN.md` เสมอ** — ติ๊ก checkbox เมื่อทำเสร็จ ถ้าจะทำนอกลำดับให้ดูหัวข้อ "ลำดับและสิ่งที่ขวางกัน" ท้ายไฟล์ก่อน

---

## กฎที่ห้ามผิด

ทั้ง 5 ข้อนี้คือบทเรียนจากระบบเดิมที่พังมาแล้ว **อย่ารื้อฟื้นโดยไม่คุยกันก่อน**

1. **ห้ามใส่ค่าชดเชยแบบ magic constant กลับเข้า `config.py`**
   เช่น `TARGET_OFFSET_*`, `Z_DROOP_COMP` — ค่าคงที่กลบความผิดพลาดที่แปรผันตามระยะไม่ได้
   ถ้ารู้สึกอยากใส่ แปลว่ามีอะไรผิดที่ต้นทาง ให้ไปแก้ที่นั่น

2. **ห้ามให้ IK เคลื่อนแขนเมื่อเป้าอยู่นอกพื้นที่ทำงาน**
   `solve_ik` ต้อง `return None` และผู้เรียกต้องจัดการ ห้ามเขียนเวอร์ชัน clamp ที่ "ยื่นไปให้ใกล้ที่สุด" เงียบๆ
   (`solve_ik_clamped` เดิมถูกลบเพราะทำให้ทั้งระบบไม่มีสัญญาณล้มเหลว)

3. **เฟสละเอียดคิดเป็นพิกเซลเท่านั้น ห้ามแปลงเป็นมิลลิเมตร**
   ถ้าเริ่มเห็นสูตรแปลง pixel→mm ในลูป fine แปลว่ากำลังกลับไปทำผิดแบบเดิม

4. **ทุกคำสั่งไปยัง servo ต้องผ่าน `arm.py`**
   ชั้นความปลอดภัย (จำกัดระยะเกิน, ความเร็ว, ขีดจำกัด joint, การชนตัวเอง) อยู่ที่นั่นที่เดียว ห้ามมีทางลัด

5. **ห้ามค้างแขนดันของแข็ง** — แตะแล้วถอยทันที
   servo ไม่รู้ว่าชนแล้ว มันจะดันจนเฟืองละลาย (ของเดิมค้างไว้ 7 วินาทีทุกรอบ)

---

## สภาพแวดล้อม

- **แก้โค้ดบน Mac → รันจริงบน Pi** ผ่าน Tailscale SSH ซิงก์ด้วย git
- `adafruit_servokit` และ `picamera2` **ติดตั้งบน Mac ไม่ได้** → `arm.py` และ `camera.py` ต้องมีโหมดจำลอง
  ที่ทำงานต่อได้เมื่อ import ไม่สำเร็จ ไม่ใช่ crash
- Python 3.11.9

## กล้องตัวเดียว — Pi Camera Module 3 Wide บนแขน (หลัง gripper)

ใช้ทั้งสองเฟส: ที่**ท่าสแกน**เห็นล้อทั้งวงสำหรับเฟสหยาบ · ตอนเข้าใกล้ใช้สำหรับเฟสละเอียด
ที่ท่าสแกนแขนอยู่ตำแหน่งที่รู้แน่นอน กล้องจึงทำหน้าที่เหมือนกล้องตรึงได้ทุกประการ

**กล้อง Logitech Brio (ตรึงกับที่) ถูกตัดออกแล้ว (2026-08)** เพราะทดสอบจริงแล้วพบว่า
เพิ่มจุดพังโดยไม่ได้เพิ่มความสามารถ — ขาตั้งขยับเองได้ (วัดได้ 23 px) และ
**แขนที่ท่าสแกนบังจุ๊บตำแหน่ง 5–6 นาฬิกา** ส่วนกล้องที่มือเห็นครบทุกตำแหน่ง

บั๊กที่เคยเกิดตอนมี 2 กล้อง: โค้ดหยิบภาพจากกล้องบนแขน ไปเข้าสูตรที่คิดว่ากล้องอยู่กับที่
— ตอนนี้เหลือกล้องเดียวจึงหมดปัญหานี้ แต่ยังต้องระวัง **ภาพที่ท่าสแกน กับ ภาพตอนเข้าใกล้
มีมาตราส่วนต่างกันมาก** อย่าเอาค่าพิกเซลจากท่าหนึ่งไปใช้กับอีกท่า

## การเขียน

- **คอมเมนต์และข้อความที่ผู้ใช้เห็น เขียนภาษาไทย** ตามสไตล์เดิมของ repo
- อธิบายศัพท์เทคนิคเสมอเมื่อใช้ครั้งแรก — เจ้าของโปรเจ็คกำลังเรียนรู้ ไม่ใช่แค่ต้องการโค้ด
