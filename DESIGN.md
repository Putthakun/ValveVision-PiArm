# DESIGN — สรุปการตัดสินใจ (rebuild 2026-08)

บันทึกจากการทบทวนโปรเจ็คทั้งหมด ก่อนเริ่มเขียนใหม่
ศัพท์ทั้งหมดในไฟล์นี้นิยามไว้ที่ [CONTEXT.md](./CONTEXT.md)

---

## 1. เป้าหมาย

**ปลายแขนไปสัมผัสก้านจุ๊บของล้อกะทะหนึ่งวง โดยใช้กล้องตรวจจับ**

- ✅ อยู่ในขอบเขต: ตรวจจับวาล์ว → คำนวณ → เคลื่อนแขนไปแตะ → วัดผลเป็นตัวเลข
- ❌ **ไม่อยู่ในขอบเขตรอบนี้:** ต่อหัวเติมลม, เติม/ปล่อยลมจริง, อ่านแรงดัน, ล้อหลายแบบ, รถจอดตำแหน่งอิสระ

**กรอบเวลา:** 8–16 สัปดาห์ (senior project มีวันสอบ)
**เกณฑ์สำเร็จ:** ปลายแขนแตะโดนก้านจุ๊บจริง — วัดด้วยกระดาษเป้า ได้ทั้ง hit-rate และระยะพลาดเป็นมิลลิเมตร

---

## 2. สถาปัตยกรรมหลัก — "หยาบเป็นมิลลิเมตร ละเอียดเป็นพิกเซล"

```
   ┌── เฟสหยาบ ──────────────┐   ┌── เฟสละเอียด ─────────┐   ┌─ ปลายตาบอด ─┐
   │  กล้องภาพรวม (ตรึง)      │   │  กล้องที่มือ           │   │             │
   │  คิดเป็น มม. แบบหยาบ      │ → │  คิดเป็น พิกเซล        │ → │  เดินตรง     │
   │  พาแขนเข้าใกล้เฉยๆ        │   │  วนจนวาล์วเข้าเป้า      │   │  ~12 ซม.    │
   │  ★ ไม่ต้องแม่น            │   │  ★ ความแม่นทั้งหมดอยู่นี่ │   │             │
   └─────────────────────────┘   └──────────────────────┘   └─────────────┘
        เลือก gripper_pitch  →  ★ ล็อกไว้ ห้ามเปลี่ยนอีกจนจบ ★
```

**เหตุผลที่เลือกแบบนี้:** ระบบเดิมแปลงพิกเซลเป็นมิลลิเมตรตั้งแต่ต้นแล้วสั่งแขนครั้งเดียวจบ
ความผิดพลาดจากทุกแหล่ง (ตำแหน่งกล้อง, มุมกล้อง, การประเมินระยะ, ความยาวแขน, การหย่อนตัวของ servo)
บวกกันแล้วไปโผล่ที่ปลายทางโดยไม่มีอะไรแก้ได้ — จึงต้องมี `TARGET_OFFSET` ไว้กลบ ซึ่งกลบได้แค่ที่จุดเดียว

ในเฟสละเอียด **ความผิดพลาดถูกลบทิ้งทุกครั้งที่ถ่ายภาพใหม่** ไม่สะสม

### กฎการควบคุมของเฟสละเอียด
- หาอัตราส่วน "ขยับกี่ มม. → ภาพเลื่อนกี่พิกเซล" ด้วยการ **วัดจริงหนึ่งครั้ง** ที่ 2–3 ระยะ
  (ได้ทั้งขนาดและ **ทิศทาง** ซึ่งเดาผิดง่ายมาก และถ้าผิดลูปจะวิ่งหนีเป้า)
- ตอนวิ่งจริง **ขยับแค่ 50% ของที่คำนวณได้ทุกรอบ** — เดาผิด 2 เท่าก็ยังลู่เข้าเป้า ไม่แกว่ง
- แก้ **ซ้าย-ขวาด้วย J1** และ **บน-ล่างด้วยความสูง z** เท่านั้น — ไม่แตะ `gripper_pitch`
  เพราะกล้องติดอยู่บนปลายแขน เปลี่ยน pitch = กล้องเงย = ภาพเลื่อนทั้งภาพ = ลูปสับสน
- **ต้องมีเงื่อนไขหยุด 3 อย่างเสมอ:** เข้าเป้าแล้ว / ครบรอบสูงสุด → ถอยกลับ / มองไม่เห็นวาล์ว → ถอยกลับ

### นโยบายเลือก `gripper_pitch`
ตอนจบเฟสหยาบ วน pitch ทีละ 5° ในช่วงที่ทำได้ เรียก `solve_ik` แล้วเลือกค่าที่ทำให้
**joint ที่คับที่สุดยังเหลือระยะขยับมากที่สุด** — ไม่ใช่เลือกที่เอื้อมได้ไกลสุด
เพราะเฟสละเอียดต้องขยับแก้อีกหลายรอบ ถ้าเลือกท่าที่ joint ชนขีดจำกัดพอดี ลูปจะค้าง

---

## 3. สิ่งที่ลบทิ้งไปแล้ว และเหตุผล

| ลบ | เหตุผล |
|---|---|
| `README.md`, `HANDOVER.md` | ขัดกับ `config.py` แทบทุกตัวเลข อ้างไฟล์โมเดลที่ไม่มีอยู่จริง |
| `main.py` | ดูหัวข้อ 4 |
| `solve_ik_clamped()` | ไม่เคยคืนค่าล้มเหลว — เป้านอกพื้นที่ทำงานก็ยังสั่งแขนไปที่ผิดเงียบๆ ทั้งระบบจึงไม่มีสัญญาณบอกว่าทำไม่ได้ |
| `CAM_*`, `FOCAL_LENGTH`, `TARGET_OFFSET_*`, `Z_DROOP_COMP` | โมเดลกล้องที่เขียนเองโดยไม่เคยทำ camera calibration จริง และค่าชดเชยที่กลบความผิดพลาดซึ่งแปรผันตามระยะไม่ได้ |
| `results/`, `captures/`, `workspace.png` | เก็บจากความยาวแขนชุดเก่าและโมเดลชุดเก่า อ่านแล้วจะสรุปผิด |
| `valvevision.service` | สั่งรัน `main.py` ที่พังอัตโนมัติตอนเปิดเครื่อง |
| `test_repeatability.py`, `setup/calibrate_target_offset.py` | สร้างมาเพื่อหาค่าชดเชยที่เราเลิกใช้แล้ว |

ทั้งหมดกู้คืนได้จาก git history

---

## 4. ข้อบกพร่องของระบบเดิม (บันทึกไว้เพื่อไม่ทำซ้ำ)

1. **ลูป visual servo เดิมไม่มีทางทำงานได้เลย** — ใช้กล้องตรึง ซึ่งเมื่อแขนขยับ ภาพไม่เปลี่ยน ค่าความคลาดเคลื่อนจึงคงที่ตลอด ลูปหยุดที่รอบสองเสมอ
2. **ประเมินระยะจากความกว้างของกรอบตรวจจับ** — ที่ระยะ 40 ซม. วาล์วกว้างราว 7 พิกเซล กรอบเพี้ยน 1 พิกเซล = ระยะเพี้ยนราว 15%
3. **`find_max_reach()` เล็งไปที่ขอบสุดของพื้นที่ทำงาน ไม่ใช่ที่วาล์ว** — เป้าอยู่ใกล้ก็เลยไปชน เป้าอยู่ไกลก็ไปไม่ถึง
4. **กล้องผิดตัว** — โค้ดเลือกกล้องที่มือมาก่อน แต่เอาไปเข้าสูตรที่คิดว่ากล้องอยู่กับที่
5. **สภาวะกลับสู่ท่าสแกนคือ "วาล์วหายไป"** ซึ่งสิ่งที่บังกล้องจริงๆ คือแขนตัวเอง — ระบบทำงานได้ด้วยเหตุผลที่ไม่ได้ตั้งใจ
6. **`HOLD_AT_TARGET_SEC = 7`** — ค้างดันของแข็งไว้ 7 วินาทีทุกรอบ ดูหัวข้อ 7
7. **ไม่มีชั้นความปลอดภัยเลย** — ไม่มีการจำกัดความเร็ว ไม่มีท่าปิดระบบ ใครก็สั่ง servo ตรงๆ ได้

---

## 5. ข้อเท็จจริงที่ยืนยันแล้ว

- **แขนเป็นแบบ serial** → โมเดล IK ใน `ik_solver.py` ถูกต้อง, `gripper_pitch` ปลดล็อกได้
- **ไฟเลี้ยง servo เพียงพอ** (ยืนยันแล้ว) — ถ้าเจออาการแขนกระตุกเมื่อไหร่ค่อยกลับมาตรวจ
- **ช่องบนล้อกว้างพอ** ให้ปลายแขนสอดเข้าถึงวาล์วได้
- **โมเดลปัจจุบันไม่มีที่มาที่ไป** — ไม่มี metadata ฝัง, ไม่มี dataset, ไม่มีโค้ดเทรน, ไม่มีผล validation ใน repo

### พื้นที่ที่เอื้อมถึงจริง (คำนวณจาก `solve_ik` ที่ไม่มีการ clamp)

> **อัปเดต 2026-08 (Task 1):** ตัวเลขด้านล่างคำนวณจาก `LIMITS` ที่วัดจริงจากแขนแล้ว
> (`J1: (10,170)`, `J2: (70,170)`, `J3: (63,170)`, `J4: (12,170)` — ดู `config.py`)
> ไม่ใช่ `(0,180)` เดิม พื้นที่ทำงานจึง**แคบกว่าที่เคยประมาณไว้มาก**

| ความสูง z | ตรึง pitch = 0 | ปลดล็อก pitch ±40° |
|---|---|---|
| 80 มม. | r = 350.6–409.7 (**59.1 มม.**) | r = 210.4–414.0 (**203.6 มม.**) |
| 120 มม. | r = 328.8–393.9 (65.1 มม.) | r = 214.8–405.2 (190.4 มม.) |
| 200 มม. | r = 245.2–329.6 (84.4 มม.) | r = 205.6–374.2 (168.6 มม.) |
| 260 มม. | **เอื้อมไม่ถึงเลย** | r = 171.4–337.2 (165.8 มม.) ✅ |

**การปลดล็อก `gripper_pitch` ขยายพื้นที่ทำงานได้ 2.0–3.4 เท่าที่ระดับความสูงต่ำ และทำให้ z=260 มม. เอื้อมถึงได้**
→ ยังคงต้องปลดล็อก `gripper_pitch` เพื่อให้พื้นที่ทำงานกว้างพอใช้จริง เหมือนที่สรุปไว้ตอนแรก
แต่ **ตัวเลขจุดวางฐาน (`r≈350, z≈100` ในหัวข้อ 6) ยังเป็นค่าจากช่วง `LIMITS` ปลอมอยู่ ต้องคำนวณใหม่ใน Task 5B ก่อนใช้จริง**

รัศมีต่ำสุดที่แคบตอน pitch=0 มาจากการที่ปลายแขนยาว 175 มม. ถูกบังคับให้ยื่นแนวนอน — ไม่ใช่เพราะแขนสั้น

---

## 6. การจัดวางฐานแขนกับล้อ

**ตัดสินใจแล้ว:** ฐานแขนกับล้อ **แยกกัน แต่มีเส้นมาร์คพื้น**

**จุดที่ควรเล็งให้วาล์วไปตก: r ≈ 350 มม., z ≈ 100 มม.** จากแกน J1
ที่จุดนี้ pitch = +15° ทำให้ joint ที่คับที่สุดยังเหลือระยะขยับถึง 44°

> ⚠️ **ตัวเลขนี้ยังเป็นค่าชั่วคราว** เพราะคำนวณจาก `LIMITS = (0,180)` ที่ยังไม่ได้วัดจริง
> ค่าจริงหาได้จาก **Task 5B ใน [PLAN.md](./PLAN.md)** ซึ่งต้องทำหลัง Task 1 (วัดขีดจำกัดจริง)
> และ **ต้องเสร็จก่อนเก็บ dataset** ไม่งั้นภาพที่เก็บมาจะไม่ตรงกับเรขาคณิตที่ใช้จริง

> ⚠️ ตัวเลข r ≈ 250, z ≈ 120 ที่เคยเสนอไว้ตอนแรก **ผิด** — จุดนั้น pitch = 0 เอื้อมไม่ถึง และ pitch ที่ดีที่สุดยังเหลือระยะขยับแค่ 23°

**ถ้าวาล์วอยู่ขอบของย่านที่เอื้อมถึง** ความคลาดเคลื่อนนิดเดียวจะดันมันหลุดออกนอกพื้นที่ทำงาน
แล้ว IK จะคืนค่า "ทำไม่ได้" ทั้งที่มองเห็นวาล์วชัดเจนอยู่ — อาการที่หาสาเหตุยากที่สุด

**ผลตามมา: ต้องวัดความเที่ยง 2 ชุดแยกกัน**

| วัดอะไร | ทำยังไง | บอกอะไร |
|---|---|---|
| ความเที่ยงของแขน | รัน 20 ครั้ง **โดยไม่แตะล้อเลย** | แขนเองนิ่งแค่ไหน |
| ความเที่ยงของการจัดฉาก | ยกล้อออกแล้ววางกลับตามเส้นมาร์ค 10 ครั้ง วัดว่าวาล์วเลื่อนกี่ มม. | เส้นมาร์คดีพอไหม |

ถ้าไม่แยก จะได้ตัวเลขก้อนเดียวที่บอกไม่ได้ว่าต้องไปแก้อะไร

---

## 7. ความปลอดภัย

**ความเสี่ยงหลัก: servo ไหม้จากการดันค้าง** — "แตะโดน" แปลว่ามีการสัมผัส
แต่ servo ไม่รู้ว่าชนแล้ว มันจะดันต่อไปเรื่อยๆ จนกระแสพุ่งและเฟืองพลาสติกละลาย
อาการไม่พังทันที แต่เฟืองจะหลวมขึ้นเรื่อยๆ ทำให้ความเที่ยงแย่ลง แล้วจะเข้าใจผิดว่าเป็นบั๊กซอฟต์แวร์

**ตัดสินใจแล้ว: จำกัดระยะเกินอย่างเดียวก่อน**
- ห้ามสั่งเกินตำแหน่งที่ตรวจจับได้เกินประมาณ 5 มม.
- แตะแล้วถอยทันที **ไม่ค้าง** (ต่างจากของเดิมที่ค้าง 7 วินาที)
- อาศัยความอ่อนตัวของแขนอลูมิเนียมเองเป็นตัวรับแรง

**ทางเลือกที่เก็บไว้ทำทีหลังถ้าจำเป็น:** ไมโครสวิตช์ที่ปลายแขน
นอกจากกัน servo ไหม้แล้ว ยังนับ hit/miss อัตโนมัติได้ → รันทดสอบ 50 รอบโดยไม่ต้องนั่งเฝ้า

**ชั้นความปลอดภัยอยู่ใน `arm.py` ที่เดียว** — จำกัดระยะเกิน, จำกัดความเร็ว, ตรวจขีดจำกัด joint,
ตรวจการชนตัวเอง, ปฏิเสธเป้าที่เอื้อมไม่ถึง — **ทุกคำสั่งต้องผ่านชั้นนี้ ไม่มีทางลัด**

---

## 8. แผนเก็บ dataset

**หลักการ: คนจัดฉาก หุ่นกวาดมุม**

| ต้องเปลี่ยน | ใครทำ | จำนวนครั้ง |
|---|---|---|
| ตำแหน่งนาฬิกาของวาล์ว | คนหมุนล้อ | ~5 |
| สภาพแสง | คนสลับไฟ | ~4 |
| มุมมอง / ระยะ / ความเอียง | **แขนกวาดเอง** | หลายสิบครั้งต่อฉาก |

20 รอบ × ~50 ภาพ ≈ 1,000 ภาพ โดยคนลงแรงจริงราว 10 นาที

**เก็บสองรอบ:**
1. กวาด grid แบบยังไม่มีโมเดล → ภาพที่ไม่มีวาล์วกลายเป็นภาพลบ ไม่มีภาพไหนเสียเปล่า
   → label ด้วยมือ ~100 ภาพ → เทรนโมเดลหยาบ
2. เอาโมเดลหยาบไปรัน pipeline จริง **บันทึกทุกเฟรมระหว่างทาง**
   → ได้ภาพที่ตรงกับสิ่งที่ระบบจะเจอจริง → ให้โมเดลทำนายกรอบล่วงหน้าแล้วไล่แก้ → เทรนใหม่

**กฎที่ห้ามผิด:**
- ต้องเก็บจาก **ล้อกะทะวงที่จะใช้สอบ** เท่านั้น (ไม่ใช่ล้อแม็ก — หน้าตาต่างกันมาก)
- ต้องมี **ภาพเบลอตอนแขนกำลังขยับ** ไม่ใช่ภาพนิ่งล้วน
- ต้องมี **ภาพรูระบายอากาศและน็อตล้อแบบไม่มีวาล์ว** เป็นภาพลบ (คล้ายวาล์วมาก)
- **แบ่ง train/val ตามรอบการเก็บ ห้ามสุ่มแบ่งรายภาพ** — เฟรมที่ติดกันเกือบเหมือนกัน สุ่มแบ่งแล้วตัวเลขจะหลอกตัวเอง

---

## 9. โครงสร้างโค้ดที่จะเขียน

```
┌─ ชั้นล่าง (มีอยู่แล้ว) ──────────────────────────────┐
│  config.py            ค่าฮาร์ดแวร์                    │
│  servo_controller.py  คุม servo                       │
│  ik_solver.py         คณิตศาสตร์ล้วน ไม่แตะฮาร์ดแวร์   │
│  valve_detector.py    โมเดลล้วน ไม่แตะกล้อง            │
└──────────────────────────────────────────────────────┘
┌─ ชั้นกลาง (เขียนใหม่) ────────────────────────────────┐
│  camera.py   กล้อง 2 ตัว หน้าตาเรียกใช้เหมือนกัน       │
│  arm.py      IK + servo + ★ความปลอดภัย + เลือก pitch  │
└──────────────────────────────────────────────────────┘
┌─ ชั้นบน ─────────────────────────────────────────────┐
│  coarse.py   เฟสหยาบ                                  │
│  fine.py     เฟสละเอียด (ลูป pixel)                   │
│  run.py      state machine ร้อยทุกอย่างเข้าด้วยกัน     │
└──────────────────────────────────────────────────────┘

tools/  collect_dataset.py, record_run.py, prelabel.py,
        measure_pixel_scale.py, find_joint_limits.py, test_repeatability.py
```

### โหมดจำลอง + workflow

**ทำทั้งคู่** — โค้ดหลักอยู่บน Mac, ซิงก์ไป Pi ด้วย git ผ่าน Tailscale SSH, รันของจริงบน Pi

`arm.py` ต้องมี **โหมดจำลอง**: ถ้า import ไลบรารี servo ไม่ได้ ให้พิมพ์ว่าจะสั่งอะไรแทนการสั่งจริง
ทุกอย่างที่เหลือ (IK, เลือก pitch, safety, state machine) ทำงานเหมือนเดิมทุกประการ

| | ตอนนี้ | มีโหมดจำลอง |
|---|---|---|
| รันบน Mac ได้ | 3 จาก 10 ไฟล์ | 9 จาก 10 ไฟล์ |

ประโยชน์จริง: ป้อนภาพที่บันทึกไว้จากการรันจริงกลับเข้าไป → เห็นว่าระบบ*จะ*ตัดสินใจยังไง →
แก้ตรรกะ → ป้อนซ้ำ **โดยไม่ต้องเข้าใกล้หุ่นและไม่เสี่ยงพังแขน**

---

## 10. การวัดผลสำหรับรายงาน

ติดกระดาษเป้าวงกลมซ้อนกัน (วงละ 5 มม.) รอบก้านจุ๊บ ทาสีที่ปลายแขน แล้วรันเต็ม pipeline ~20 ครั้ง
รอยจุดบนกระดาษให้ทั้ง **ความแม่น** (กลุ่มจุดเบี่ยงไปทางไหน) และ **ความเที่ยง** (จุดกระจายกว้างแค่ไหน)

> ความแม่นแย่ = ปัญหาอยู่ที่ calibration แก้ด้วยซอฟต์แวร์ได้
> ความเที่ยงแย่ = ปัญหาเชิงกล แก้ด้วยซอฟต์แวร์ไม่ได้
> จึงต้องรายงานแยกกันเสมอ

---

## 11. สิ่งที่ต้องตรวจก่อนเริ่มเขียนโค้ด

| # | เรื่อง | วิธีตรวจ | ถ้าผลเป็นลบ |
|---|---|---|---|
| 1 | **ขีดจำกัดจริงของแต่ละ joint** | ค่อยๆ jog ทีละ joint จนเริ่มชนโครงแขน แล้วจดไว้ | `LIMITS` ใน config ตั้งไว้ (0,180) ทุกตัว ซึ่งเป็นขีดจำกัด **servo** ไม่ใช่ของ **โครงแขนจริง** → ตัวเลข "เหลือระยะขยับ 44°" ในหัวข้อ 6 อาจเหลือจริงแค่ 15° และท่าที่คำนวณว่าทำได้ อาจเป็นท่าที่แขนชนตัวเองพัง |
| 2 | **กล้องที่มือเห็นปลาย gripper ไหม** | เปิดสตรีมดู | เห็น → เทียบตำแหน่งสองสิ่งในภาพเดียวกัน **ไม่ต้อง calibrate เลย** / ไม่เห็น → ต้องหา "จุดเล็ง" ด้วยการทดลอง 2–3 ระยะ |
| 3 | **โมเดลปัจจุบันเจอวาล์วที่ระยะ 12–15 ซม. ไหม** | ถ่าย ~20 ภาพจากกล้องที่มือ แล้วรันโมเดล | เจอไม่ถึงครึ่ง → ต้องเก็บ dataset ใหม่ก่อน (อยู่ในแผนอยู่แล้ว) |
| 4 | **ความเที่ยงของแขนเป็นเท่าไหร่** | ยังไม่มีข้อมูลที่ใช้ได้เลย ชุดเก่า 2 ใน 10 ครั้งรายงานค่าเกินระยะที่แขนเอื้อมถึงได้ = **วิธีวัดพัง ไม่ใช่แขนพัง** | ต้องวัดใหม่ด้วยวิธีที่เชื่อถือได้ ก่อนตั้งเป้าความแม่นยำใดๆ |

> **ข้อ 1 ต้องทำก่อนเพื่อน** — ตัวเลข workspace ทั้งหมดในเอกสารนี้ตั้งอยู่บน `LIMITS` ที่ยังไม่ได้ยืนยัน

---

## 12. ไฟล์ที่ยังเหลืออยู่

| ไฟล์ | สถานะ |
|---|---|
| `config.py` | เก็บเฉพาะค่าฮาร์ดแวร์ ค่าที่เกี่ยวกับกล้องถูกลบหมดแล้ว |
| `servo_controller.py` | ใช้ได้ ยังไม่มีชั้นความปลอดภัยและโหมดจำลอง |
| `ik_solver.py` | โมเดลถูกต้อง (แขนเป็น serial) เหลือ `solve_ik` กับ `fk` |
| `valve_detector.py` | ส่วนอนุมานผลโมเดล ใช้ได้ แยกจากกล้องเรียบร้อยดี |
| `camera_preview.py` | ยังตั้งค่าไว้ใช้กล้องภาพรวมเท่านั้น ต้องแก้ให้รองรับกล้องที่มือ |
| `test_ik_servo.py` | เครื่องมือป้อนพิกัดด้วยมือ ตอนนี้ปฏิเสธเป้าที่เอื้อมไม่ถึงแทนที่จะฝืนไป |
| `workspace_map.py` | วาดแผนภาพพื้นที่ทำงาน |
| `setup/step1-3`, `set_scan_pose.py` | ขั้นตอน calibrate servo ยังใช้ได้ |
