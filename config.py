# config.py — ตั้งค่าแขนกล 6-DOF

# ── Link Lengths (mm) วัดจาก pivot ถึง pivot ──────────────────────────────
# วัดจริงจากแขน (Task 2, rebuild 2026-08) — ยืนยันด้วย FK: fk(90,90,90)
# ให้ z=460 ตรงกับความสูงที่วัดได้จริง (460±2 มม.) ที่ท่าตั้งตรง
L1 = 25    # J1 → J2  (ความสูง base ถึง shoulder)
L2 = 105   # J2 → J3  (shoulder ถึง elbow)
L3 = 140   # J3 → J4  (elbow ถึง wrist)
L4 = 190   # J4 → ปลาย gripper

# ── Channel Map (PCA9685) ──────────────────────────────────────────────────
CHANNEL = {
    'J1': 0,   # Base     (หมุนซ้าย-ขวา)
    'J2': 1,   # Shoulder (ยก-กด)
    'J3': 2,   # Elbow
    'J4': 3,   # Wrist pitch
    'J5': 4,   # Wrist roll
    'J6': 5,   # Gripper
}

# ── Pulse Range (µs) ───────────────────────────────────────────────────────
PULSE_MIN = 500
PULSE_MAX = 2500

# ── Invert — กรอกหลังรัน step2_test_direction.py ─────────────────────────
# False = เพิ่ม angle → servo หมุนตามเข็ม
# True  = เพิ่ม angle → servo หมุนทวนเข็ม (พลิก)
INVERT = {
    'J1': True,
    'J2': True,
    'J3': False,
    'J4': False,
    'J5': False,
    'J6': False,
}

# ── Zero Offset (องศา) — ปรับหลัง step3_calibrate ────────────────────────
# ใช้แก้ถ้า servo อยู่ที่ 90° แล้วแขนไม่ตั้งตรงพอดี
ZERO_OFFSET = {
    'J1': 0, 'J2': 9, 'J3': 4,
    'J4': -3, 'J5': 0, 'J6': 0,
}

# ── Joint Limits (องศา) — วัดจริงจากแขนด้วย tools/find_joint_limits.py ────
# (Task 1, rebuild 2026-08) หักระยะปลอดภัย 10° เข้ามาจากจุดที่ชนจริงแล้ว
# J5, J6 ไม่ได้ใช้งาน ยังไม่ได้วัด คงค่าเดิมไว้ (0,180)
LIMITS = {
    'J1': (10,   170),
    'J2': (70,   170),
    'J3': (63,   170),
    'J4': (12,   170),
    'J5': (0,   180),
    'J6': (0,   180),
}

# ── Home Pose (องศา) — ทุก joint ตั้งตรง ─────────────────────────────────
HOME = {
    'J1': 90,
    'J2': 90,
    'J3': 90,
    'J4': 90,
    'J5': 90,
    'J6': 90,
}


# ── Scan Pose (องศา) — ท่าที่กล้องที่มือมองหาจุ๊บ ────────────────────────
# ★ มี 2 ท่า เพราะเฟรมเดียวครอบวงจุ๊บทั้งวงไม่ได้
#   เฟสหยาบไปท่า A ก่อน ถ้าไม่เจอค่อยไปท่า B (เพิ่มเวลา ~3 วินาที)
#
#   ท่า A (J2=48) กล้องก้มลง  → เห็นราว 2–10 นาฬิกา (ครึ่งล่าง)
#   ท่า B (J2=36) กล้องเงยขึ้น → เห็นราว 8–4 นาฬิกา ผ่าน 12 (ครึ่งบน)
#   ซ้อนทับกันที่ 2–4 และ 8–10 นาฬิกา จึงไม่มีช่องโหว่
#
# หาจากการทดลองจริง (2026-08) หมุนจุ๊บไป 6 กับ 12 นาฬิกาแล้วกวาดค่า J2:
#   ท่า A: J2=44 จุ๊บ 6 นาฬิกาชิด gripper · J2=48 โล่งทั้งสองแกน ← เลือก
#   ท่า B: J2=39 จุ๊บ 12 นาฬิกาห่างขอบบนแค่ 17 px · J2=36 ห่าง 96 px ← เลือก
#
#   J1 น้อยลง = ล้อเลื่อนไปทางขวาในภาพ
#   J2 มากขึ้น = กล้องก้มลง (ล้อเลื่อนขึ้นในภาพ) · น้อยลง = เงยขึ้น
#
# ★ วงจุ๊บในภาพเป็น "วงรี" ด้านบนรัศมีใหญ่กว่าด้านล่าง — อย่าคำนวณเป็นวงกลม
#   เคยสรุปผิดมาแล้วว่า 12 นาฬิกาอยู่ในเฟรมทั้งที่หลุดไปแล้ว
#
# ⚠️ ทั้งสองท่าอยู่นอก LIMITS ข้างบนโดยตั้งใจ — LIMITS วัดตอนแขนตั้งตรง 90°
# ซึ่งเป็นท่าที่ J2 ไปฟาดที่ยึดกล้องด้านหลัง แต่ท่าสแกนพับ J3 มาข้างหน้าแล้ว
# จึงไม่ชน (ยืนยันด้วยการทดลองจริง) การชนตัวเองขึ้นกับท่าของแกนอื่นด้วย

SCAN_POSE = {          # ท่า A — ครึ่งล่าง (ท่าเริ่มต้น)
    'J1': 85.0,
    'J2': 48.0,
    'J3': 180.0,
    'J4': 180.0,
    'J5': 90.0,
    'J6': 90.0,
}

SCAN_POSE_UPPER = {    # ท่า B — ครึ่งบน (ใช้เมื่อท่า A หาจุ๊บไม่เจอ)
    'J1': 85.0,
    'J2': 36.0,
    'J3': 180.0,
    'J4': 180.0,
    'J5': 90.0,
    'J6': 90.0,
}
